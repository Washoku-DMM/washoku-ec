<h2>注文情報入力</h2>

<%= form_with model: @order, url: admins_genres_path do |f| %>

<h3>支払方法</h3>

<%= f.radio_button :status, :enabled, checked: "checked" %>クレジットカード
 <%= f.radio_button :status, :disabled, checked: "checked" %>銀行振込


<h3>お届け先</h3>

	<h3>ご自身の住所</h3>
		<!-- 頭に○クリック実装する --><%   %>

	<h3>登録済住所から選択</h3>
		<!-- 頭に○クリック実装する -->
		<!-- derivery.adressをプルダウンで一覧表示 -->

	<!-- 頭に○クリック実装する --><h3>新しいお届け先</h3>
	<!-- ○クリックを押すと、登録するボタンが出てきて、createアクションができるようにする -->

		<%= form_for(@order) do |f| %>

	        <div class="field">
	          <%= f.label "郵便番号（ハイフンなし）"  %>
	          <%= f.text_field :postal_code, autofocus: true %>
	        </div>

	        <div class="field">
	          <%= f.label　"住所"  %>
	          <%= f.text_area :adress, autofocus: true %>
	        </div>

	        <div class="field">
	          <%= f.label　"宛名"  %>
	          <%= f.text_field :name, autofocus: true, class: "name" %>
	        </div>

        <% end %>

        <div class="actions">
           <%= f.submit "確認画面へ進む",class: "btn btn-success" %>
        </div>


