<!-- サイドバーのジャンルは部分テンプレートを挿入 -->
<h1>商品詳細</h1> <h4>(全</h4><%= @product.count %> <h4>件</h4>

<% @lists.each do |product| %>
<%= attachment_image_tag @product, :image, :fill,300,300,format:'jpeg'  %>
<%= link_to product.name,admin_product_path(product.id) %>
<%= @product.price %>

<% end %>


<!-- 商品詳細で画面に収まらなかったものを横にスクロールさせて表示させる？
一覧の下にページ数を表す点が4つある -->

