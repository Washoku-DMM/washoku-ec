<!-- header -->
<%= render 'admins/header' %>
<!-- サイドバーのジャンルは部分テンプレートを挿入 -->
<%= render 'products/genres_search' %>


<% @products.each do |product| %>
<%= attachment_image_tag product, :image, :fill,300,300,format:'jpeg',fallback: "washoku.jpg", size:'300x300'  %>
<%= link_to product.name, product_path(product) %>
<%= product.price %>

<% end %>


<!-- 商品詳細で画面に収まらなかったものを横にスクロールさせて表示させる？
一覧の下にページ数を表す点が4つある -->

<%= will_paginate @products, previous_label: '&lt 前へ', next_label: '次へ &gt' %>
